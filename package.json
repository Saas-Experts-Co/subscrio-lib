{
  "name": "subscrio-workspace",
  "private": true,
  "workspaces": [
    "core.typescript",
    "server"
  ],
  "scripts": {
    "build:core": "npm run build --workspace=core.typescript",
    "build:all": "npm run build --workspaces",
    "test:core": "npm run test --workspace=core.typescript",
    "dev:server": "npm run dev --workspace=server"
  }
}
