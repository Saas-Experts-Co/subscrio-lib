# For NPM Private Registry (recommended)
@subscrio:registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# For GitHub Packages (alternative)
# @subscrio:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# For Verdaccio (self-hosted)
# registry=http://localhost:4873
